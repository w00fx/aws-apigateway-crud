{
  "name": "aws-apigateway-crud",
  "version": "1.0.0",
  "description": "A simple Notes application using CRUD, created with API Gateway, AWS Lambda and DynamoDB.",
  "scripts": {
    "test": "sls invoke test",
    "integration-test": "mocha integration_tests/"
  },
  "devDependencies": {
    "aws-sdk": "^2.1268.0",
    "aws-sdk-mock": "^5.8.0",
    "axios": "^1.2.1",
    "chai": "^4.3.7",
    "dotenv": "^16.0.3",
    "eslint": "^8.29.0",
    "mocha": "^10.2.0",
    "prettier": "^2.8.1",
    "serverless-export-outputs": "^1.0.2",
    "serverless-iam-roles-per-function": "^3.2.0",
    "serverless-mocha-plugin": "^1.12.0"
  }
}
